{
  "build": {
    "buildCommand": "cd Django-Project-AI/expense_tracker && pip install -r requirements.txt"
  },
  "start": {
    "command": "cd Django-Project-AI/expense_tracker && gunicorn expense_tracker.wsgi"
  }
}
