{
  "build": {
    "builder": "paketo-buildpacks/python",
    "buildCommand": "cd Django-Project-AI/expense_tracker && pip install -r requirements.txt",
    "startCommand": "cd Django-Project-AI/expense_tracker && gunicorn expense_tracker.wsgi"
  }
}
